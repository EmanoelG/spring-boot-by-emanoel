server:
   port: 8888
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:mysql://localhost:5432/bucetinha?useTimezone=true&serverTimezone=UTC
    username: rpdv
    password: rpdvwin1064
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
springdoc:
  pathsToMatch: /api/**/v1/**
  swagger-ui:
    use-root-path: true
    
   #org.postgresql.Driver
#jdbc:postgresql://localhost:5432/bucetinha
#rpdv
#rpdvwin1064